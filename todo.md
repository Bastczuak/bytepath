-game crashes when too many entities are rendered because
the buffers exceeds the max set size.
-make everything configurable (serde/resource)
-replace every magic number with a configurable value in config
